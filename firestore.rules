rules_version = '2';

// Toda la data se accede solo via Cloud Functions (Admin SDK).
// El frontend NO accede directo a Firestore.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
